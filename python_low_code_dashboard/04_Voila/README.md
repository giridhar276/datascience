# Voilà Examples

Launch any notebook as a standalone dashboard:

```bash
voila 01_executive_widget_dashboard.ipynb
```

The notebooks use ipywidgets and Plotly. Voilà hides the code by default and serves the interactive outputs as a web application.
