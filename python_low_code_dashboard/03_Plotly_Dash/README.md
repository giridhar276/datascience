# Plotly Dash Examples

Run an example with:

```bash
python 01_crossfilter_portfolio.py
```

Then open the local URL shown in the terminal. The five examples demonstrate callbacks, cross-filtering, scenario modelling, tabbed layouts, editable tables and downloadable data.
