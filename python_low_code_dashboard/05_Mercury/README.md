# Mercury Examples

Mercury detects notebooks in this folder and serves them as web apps.

From this folder, run:

```bash
mercury run
```

Depending on the installed Mercury version, `mercury watch` may also be available for development preview.

Each notebook contains a raw YAML configuration cell. Mercury converts these controls into web inputs and reruns the notebook with the selected values.
